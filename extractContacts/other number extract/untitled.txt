directory = 'C:\\Users\\Yan\\Desktop\\git\\scannedFiles'
os.chdir(directory)
for files in os.listdir():
    if files.endswith('.txt'):
        with open(str(files),  encoding='UTF-8') as txtFile:
            listString = txtFile.readlines()
            for i in range(len(listString)):
                for j in range(len(listString[i])):
                    for t in range(len(listString[j])):
                        print(listString[j][t])